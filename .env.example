DATABASE_URL=postgresql://user:password@localhost:5432/esexpress_v2
JWT_SECRET=change-me-to-a-32-char-minimum-secret
PORT=3000
NODE_ENV=development

# PropX API
PROPX_API_KEY=
PROPX_API_URL=https://publicapis.propx.com/api/v1
PROPX_CONCURRENCY=2
PROPX_INTERVAL_MS=1000
PROPX_INTERVAL_CAP=5

# PCS Express — shared config (SOAP + REST)
PCS_COMPANY_ID=
PCS_COMPANY_NAME=
PCS_COMPANY_LTR=
PCS_DISPATCH_ENABLED=false

# PCS Express (legacy SOAP — still used for status/clear-routes/messaging)
PCS_ACCESS_KEY=

# PCS Express (REST — Kyle Puryear 2026-04-16)
# Base URL; /authorization/v1 and /dispatching/v1 hang off this.
PCS_BASE_URL=https://api.pcssoft.com
# Optional sandbox override — set to non-prod URL once PCS provides one.
PCS_SANDBOX_URL=
# OAuth2 client-credentials — issued via 1Password share.
PCS_CLIENT_ID=
PCS_CLIENT_SECRET=

# JotForm
JOTFORM_API_KEY=
JOTFORM_FORM_ID=240655800307047

# LogistiqIQ Carrier Export
LOGISTIQ_API_KEY=
LOGISTIQ_CARRIER_ID=413
LOGISTIQ_API_URL=https://fhll67llek.execute-api.us-east-1.amazonaws.com/prod

# Google (service account JSON loaded via setup script or DB)
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
